spring:
  application:
    name: swagger-api-gateway-server

server:
  port: 11000

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
management:
  security:
    enabled: false
